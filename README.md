# 📚 API de Aprendizagem (.NET 8 + JWT)

Esta API foi criada com foco em **estudo e prática** de desenvolvimento backend utilizando:

- **.NET 8**
- **Entity Framework Core**
- **SQL Server**
- **Autenticação e Autorização com JWT (JSON Web Token)**

O objetivo é servir como um laboratório para aprender como estruturar uma API real, com **camadas bem definidas**, **persistência de dados** e **segurança nas rotas**.

---

## 🚀 Funcionalidades (planejadas / em desenvolvimento)

- Cadastro e autenticação de usuários
- Geração de **JWT** na autenticação
- Rotas protegidas que exigem **Bearer Token**
- CRUD de entidades de exemplo (ex: Tarefas / Cursos / Itens de Estudo)
- Controle de acesso baseado em **perfis** (ex: `Admin` e `User`)

---

## 🧠 Tecnologias Utilizadas

- **Linguagem:** C# (.NET 8)
- **Framework Web:** ASP.NET Core Controllers
- **ORM:** Entity Framework Core
- **Banco de Dados:** SQL Server
- **Autenticação:** JWT Bearer
- **Migrações:** `dotnet ef`
